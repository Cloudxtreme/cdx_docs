wajig install mailgraph rrdtool


  apache2-mpm-itk apache2-utils apache2.2-bin apache2.2-common defoma fontconfig fontconfig-config libaprutil1 libaprutil1-dbd-sqlite3 libaprutil1-ldap libcairo2 libdatrie1
  libdirectfb-1.2-0 libfile-tail-perl libfontconfig1 libfontenc1 libpango1.0-0 libpango1.0-common libpixman-1-0 librrd4 librrds-perl libsysfs2 libthai-data libthai0 libts-0.0-0
  libxcb-render-util0 libxcb-render0 libxfont1 libxft2 libxrender1 mailgraph rrdtool tsconf ttf-dejavu ttf-dejavu-core ttf-dejavu-extra x-ttcidfont-conf x11-common xfonts-encodings
  xfonts-utils

libapache2-mod-perl2 libapache2-reload-perl libbsd-resource-perl libdevel-symdump-perl libperl5.10


--
-- syslog-ng config:
--
options { sync (0);
  time_reopen (10);
  log_fifo_size (1000);
  long_hostnames (off);
  use_dns (no);
  use_fqdn (no);
  create_dirs (no);
  keep_hostname (yes);
};

source s_all {
  file ("/proc/kmsg" log_prefix("kernel: "));
  unix-stream ("/dev/log");
  internal();
};


destination du_all { usertty("*"); };
destination df_loghost { udp("syslog" port(514)); };

destination df_mail { file("/var/log/mail.log"); };
filter f_mail { facility(mail); };

log { source(s_all); filter(f_mail); destination(df_mail); };
log { source(s_all); destination(df_loghost); };

~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

See apache2/perl.txt

http://mail.cardiodx.com/cgi-bin/mailgraph.cgi

service mailgraph restart



