<%= form_for(@admin_collection) do |f| %>
  <% if @admin_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_collection.errors.count, "error") %> prohibited this admin_collection from being saved:</h2>

      <ul>
      <% @admin_collection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
