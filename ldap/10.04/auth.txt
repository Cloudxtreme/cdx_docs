
  260  wajig install libnss-ldap
    example.coom
    cn=admin,dc=example,dc=com

dpkg-reconfigure ldap-auth-config

  261  auth-client-config -t nss -p lac_ldap
  262  pam-auth-update


apt-get install ldapscripts

vi /etc/ldapscripts/ldapscripts.conf

SERVER="ldap://localhost"
BINDDN="cn=admin,dc=cardiodx,dc=com"
BINDPWDFILE="/etc/ldapscripts/ldapscripts.passwd"
SUFFIX="dc=cardiodx,dc=com" # Global suffix
GSUFFIX="ou=Groups"        # Groups ou (just under $SUFFIX)
USUFFIX="ou=Users"         # Users ou (just under $SUFFIX)
MSUFFIX="ou=Computers"      # Machines ou (just under $SUFFIX)
GIDSTART="10000" # Group ID
UIDSTART="10000" # User ID
MIDSTART="10000" # Machine ID


  266  sh -c "echo -n 'secret' > /etc/ldapscripts/ldapscripts.passwd"
  267  chmod 400 /etc/ldapscripts/ldapscripts.passwd
  268  ldapadduser george example
  269  ldapsetpasswd george

  271  ldapaddgroup qa
  272  ldapaddusertogroup george qa

$ ssh george@ldaptest

$ id
uid=10001(george) gid=10000(example) groups=10000(example),10001(qa)

    2  getent passwd
    3  getent group

